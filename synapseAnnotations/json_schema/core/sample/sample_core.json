{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id": "",
    "description": "A sample entity contains information about biological material that was generated/used in the project and includes everything from a whole organism down to subcellular components.",
    "additionalProperties": false,
    "required": [
        "specimenID",
        "species"
    ],
    "title": "sample",
    "type": "object",
    "properties": {
        "describedBy": {
            "description": "The URL reference to the schema.",
            "type": "string",
            "pattern" : ""
        },
        "schema_version": {
            "description": "The version number of the schema in major.minor.patch format.",
            "type": "string",
            "pattern": "^[0-9]{1,}.[0-9]{1,}.[0-9]{1,}$",
            "example": "4.6.1"
        },
        "specimenID":{
            "description": "A unique ID for this sample.",
            "type": "string",
            "user_friendly": "Specimen ID"
        },
        "specimenName": {
            "description": "A short, descriptive name for the sample that need not be unique.",
            "type": "string",
            "user_friendly": "Biomaterial name"
        },
        "specimenDescription": {
            "description": "A general description of the biomaterial.",
            "type": "string",
            "user_friendly": "Biomaterial description"
        },
        "species" : {
            "description": "A species name from MESH, can be multiple.",
            "type" : "array",
            "items":{
                "type": "string"
            },
            "user_friendly": "species name"
        },
        "tissue" :{
            "description": "Tissue from which sample was derived",
            "type": "string",
            "user_friendly": "Tissue description"
        },
        "organ" :{
            "description": "Organ from which sample was derived.",
            "type": "string",
            "user_friendly": "Organ description"
        }
    }
}
